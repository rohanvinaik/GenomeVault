from typing import Any, Dict

import torch

from . import _dtypes

# mypy: ignore-errors




def finfo(dtyp) -> None:
        """TODO: Add docstring for finfo"""
    torch_dtype = _dtypes.dtype(dtyp).torch_dtype
    return torch.finfo(torch_dtype)


def iinfo(dtyp) -> None:
        """TODO: Add docstring for iinfo"""
    torch_dtype = _dtypes.dtype(dtyp).torch_dtype
    return torch.iinfo(torch_dtype)

