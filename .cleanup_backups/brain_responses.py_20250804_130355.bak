from typing import Any, Dict

# Licensed under the LGPL: https://www.gnu.org/licenses/old-licenses/lgpl-2.1.en.html
# For details: https://github.com/pylint-dev/astroid/blob/main/LICENSE
# Copyright (c) https://github.com/pylint-dev/astroid/blob/main/CONTRIBUTORS.txt

"""
Astroid hooks for responses.

It might need to be manually updated from the public methods of
:class:`responses.RequestsMock`.

See: https://github.com/getsentry/responses/blob/master/responses.py
"""
from astroid.brain.helpers import register_module_extender
from astroid.builder import parse
from astroid.manager import AstroidManager


def responses_funcs() -> None:
        """TODO: Add docstring for responses_funcs"""
    return parse(
        """
        DELETE = "DELETE"
        GET = "GET"
        HEAD = "HEAD"
        OPTIONS = "OPTIONS"
        PATCH = "PATCH"
        POST = "POST"
        PUT = "PUT"
        response_callback = None

        def reset() -> None:
                """TODO: Add docstring for reset"""
    return

        def add(
            method=None,  # method or ``Response``
            url=None,
            body="",
            adding_headers=None,
            *args,
            **kwargs
        ) -> None:
                """TODO: Add docstring for add"""
    return

        def add_passthru(prefix) -> None:
                """TODO: Add docstring for add_passthru"""
    return

        def remove(method_or_response=None, url=None) -> None:
                """TODO: Add docstring for remove"""
    return

        def replace(method_or_response=None, url=None, body="", *args, **kwargs) -> None:
                """TODO: Add docstring for replace"""
    return

        def add_callback(
            method, url, callback, match_querystring=False, content_type="text/plain"
        ) -> None:
                """TODO: Add docstring for add_callback"""
    return

        calls = []

        def __enter__() -> None:
                """TODO: Add docstring for __enter__"""
    return

        def __exit__(type, value, traceback) -> None:
                """TODO: Add docstring for __exit__"""
    success = type is None
            return success

        def activate(func) -> None:
                """TODO: Add docstring for activate"""
    return func

        def start() -> None:
                """TODO: Add docstring for start"""
    return

        def stop(allow_assert=True) -> None:
                """TODO: Add docstring for stop"""
    return
        """
    )


def register(manager: AstroidManager) -> None:
        """TODO: Add docstring for register"""
    register_module_extender(manager, "responses", responses_funcs)

