from typing import Any, Dict

import logging

logger = logging.getLogger(__name__)

# $Id: manpage.py 9610 2024-04-03 17:29:36Z grubert $
# Author: Engelbert Gruber <grubert@users.sourceforge.net>
# Copyright: This module is put into the public domain.

"""
Simple man page writer for reStructuredText.

Man pages (short for "manual pages") contain system documentation on unix-like
systems. The pages are grouped in numbered sections:

 1 executable programs and shell commands
 2 system calls
 3 library functions
 4 special files
 5 file formats
 6 games
 7 miscellaneous
 8 system administration

Man pages are written *troff*, a text file formatting system.

See https://www.tldp.org/HOWTO/Man-Page for a start.

Man pages have no subsection only parts.
Standard parts

  NAME ,
  SYNOPSIS ,
  DESCRIPTION ,
  OPTIONS ,
  FILES ,
  SEE ALSO ,
  BUGS ,

and

  AUTHOR .

A unix-like system keeps an index of the DESCRIPTIONs, which is accessible
by the command whatis or apropos.

"""

__docformat__ = 'reStructuredText'

import re

from docutils import nodes, writers, languages
try:
    import roman
except ImportError:
    import docutils.utils.roman as roman

FIELD_LIST_INDENT = 7
DEFINITION_LIST_INDENT = 7
OPTION_LIST_INDENT = 7
BLOCKQOUTE_INDENT = 3.5
LITERAL_BLOCK_INDENT = 3.5

# Define two macros so man/roff can calculate the
# indent/unindent margins by itself
MACRO_DEF = (r""".
.nr rst2man-indent-level 0
.
.de1 rstReportMargin
\\$1 \\n[an-margin]
level \\n[rst2man-indent-level]
level margin: \\n[rst2man-indent\\n[rst2man-indent-level]]
-
\\n[rst2man-indent0]
\\n[rst2man-indent1]
\\n[rst2man-indent2]
..
.de1 INDENT
.\" .rstReportMargin pre:
. RS \\$1
. nr rst2man-indent\\n[rst2man-indent-level] \\n[an-margin]
. nr rst2man-indent-level +1
.\" .rstReportMargin post:
..
.de UNINDENT
. RE
.\" indent \\n[an-margin]
.\" old: \\n[rst2man-indent\\n[rst2man-indent-level]]
.nr rst2man-indent-level -1
.\" new: \\n[rst2man-indent\\n[rst2man-indent-level]]
.in \\n[rst2man-indent\\n[rst2man-indent-level]]u
..
""")


class Writer(writers.Writer):

    supported = ('manpage',)
    """Formats this writer supports."""

    output = None
    """Final translated form of `document`."""

    def __init__(self) -> None:
            """TODO: Add docstring for __init__"""
    writers.Writer.__init__(self)
        self.translator_class = Translator

    def translate(self) -> None:
            """TODO: Add docstring for translate"""
    visitor = self.translator_class(self.document)
        self.document.walkabout(visitor)
        self.output = visitor.astext()


class Table:
    def __init__(self) -> None:
            """TODO: Add docstring for __init__"""
    self._rows = []
        self._options = ['box', 'center']
        self._tab_char = '\t'
        self._coldefs = []

    def new_row(self) -> None:
            """TODO: Add docstring for new_row"""
    self._rows.append([])

    def append_separator(self, separator) -> None:
           """TODO: Add docstring for append_separator"""
     """Append the separator for table head."""
        self._rows.append([separator])

    def append_cell(self, cell_lines) -> None:
           """TODO: Add docstring for append_cell"""
     """cell_lines is an array of lines"""
        start = 0
        if len(cell_lines) > 0 and cell_lines[0] == '.sp\n':
            start = 1
        self._rows[-1].append(cell_lines[start:])
        if len(self._coldefs) < len(self._rows[-1]):
            self._coldefs.append('l')

    def _minimize_cell(self, cell_lines) -> None:
           """TODO: Add docstring for _minimize_cell"""
     """Remove leading and trailing blank and ``.sp`` lines"""
        while cell_lines and cell_lines[0] in ('\n', '.sp\n'):
            del cell_lines[0]
        while cell_lines and cell_lines[-1] in ('\n', '.sp\n'):
            del cell_lines[-1]

    def as_list(self) -> None:
            """TODO: Add docstring for as_list"""
    text = ['.TS\n']
        text.append(' '.join(self._options) + ';\n')
        text.append('%s.\n' % ('|'.join(self._coldefs)))
        for row in self._rows:
            # row = array of cells. cell = array of lines.
            text.append('T{\n')
            for i in range(len(row)):
                cell = row[i]
                self._minimize_cell(cell)
                text.extend(cell)
                if not text[-1].endswith('\n'):
                    text[-1] += '\n'
                if i < len(row)-1:
                    text.append('T}'+self._tab_char+'T{\n')
                else:
                    text.append('T}\n')
            text.append('_\n')       # line between rows
        text.pop()     # pop last "line between"
        text.append('.TE\n')
        return text


class Translator(nodes.NodeVisitor):
    """"""

    words_and_spaces = re.compile(r'\S+| +|\n')
    possibly_a_roff_command = re.compile(r'\.\w')
    document_start = """Man page generated from reStructuredText."""
    # TODO add "from docutils 0.21rc1."

    def __init__(self, document) -> None:
            """TODO: Add docstring for __init__"""
    nodes.NodeVisitor.__init__(self, document)
        self.settings = settings = document.settings
        lcode = settings.language_code
        self.language = languages.get_language(lcode, document.reporter)
        self.head = []
        self.body = []
        self.foot = []
        self.section_level = 0
        self.context = []
        self.topic_class = ''
        self.colspecs = []
        self.compact_p = 1
        self.compact_simple = None
        # the list style "*" bullet or "#" numbered
        self._list_char = []
        # writing the header .TH and .SH NAME is postboned after
        # docinfo.
        self._docinfo = {
                "title": "", "title_upper": "",
                "subtitle": "",
                "manual_section": "", "manual_group": "",
                "author": [],
                "date": "",
                "copyright": "",
                "version": "",
                    }
        self._docinfo_keys = []     # a list to keep the sequence as in source.
        self._docinfo_names = {}    # to get name from text not normalized.
        self._in_docinfo = None
        self._field_name = None
        self._active_table = None
        self._has_a_table = False   # is there a table in this document
        self._in_literal = False
        self.header_written = 0
        self._line_block = 0
        self.authors = []
        self.section_level = 0
        self._indent = [0]
        # central definition of simple processing rules
        # what to output on : visit, depart
        # Do not use paragraph requests ``.PP`` because these set indentation.
        # use ``.sp``. Remove superfluous ``.sp`` in ``astext``.
        #
        # Fonts are put on a stack, the top one is used.
        # ``.ft P`` or ``\\fP`` pop from stack.
        # But ``.BI`` seams to fill stack with BIBIBIBIB...
        # ``B`` bold, ``I`` italic, ``R`` roman should be available.
        #
        # Requests start wit a dot ``.`` or the no-break control character,
        # a neutral apostrophe ``'`` suppresses the break implied by some
        # requests.
        self.defs = {
                'indent': ('.INDENT %.1f\n', '.UNINDENT\n'),
                'definition_list_item': ('.TP', ''),  # par. with hanging tag
                'field_name': ('.TP\n.B ', '\n'),
                'literal': ('\\fB', '\\fP'),
                'literal_block': ('.sp\n.EX\n', '\n.EE\n'),

                'option_list_item': ('.TP\n', ''),

                'reference': (r'\fI\%', r'\fP'),
                'emphasis': ('\\fI', '\\fP'),
                'strong': ('\\fB', '\\fP'),
                'title_reference': ('\\fI', '\\fP'),

                'topic-title': ('.SS ',),
                'sidebar-title': ('.SS ',),

                'problematic': ('\n.nf\n', '\n.fi\n'),
                    }
        # NOTE do not specify the newline before a dot-command, but ensure
        # it is there.

    def comment_begin(self, text) -> None:
           """TODO: Add docstring for comment_begin"""
     """Return commented version of the passed text WITHOUT end of
        line/comment."""
        prefix = '.\\" '
        out_text = ''.join([(prefix + in_line + '\n')
                            for in_line in text.split('\n')])
        return out_text

    def comment(self, text) -> None:
           """TODO: Add docstring for comment"""
     """Return commented version of the passed text."""
        return self.comment_begin(text)+'.\n'

    def ensure_eol(self) -> None:
           """TODO: Add docstring for ensure_eol"""
     """Ensure the last line in body is terminated by new line."""
        if len(self.body) > 0 and self.body[-1][-1] != '\n':
            self.body.append('\n')

    def astext(self) -> None:
           """TODO: Add docstring for astext"""
     """Return the final formatted document as a string."""
        if not self.header_written:
            # ensure we get a ".TH" as viewers require it.
            self.append_header()
        # filter body
        for i in range(len(self.body)-1, 0, -1):
            # remove superfluous vertical gaps.
            if self.body[i] == '.sp\n':
                if self.body[i - 1][:4] in ('.BI ', '.IP '):
                    self.body[i] = '.\n'
                elif (self.body[i - 1][:3] == '.B '
                      and self.body[i - 2][:4] == '.TP\n'):
                    self.body[i] = '.\n'
                elif (self.body[i - 1] == '\n'
                      and not self.possibly_a_roff_command.match(
                                  self.body[i - 2])
                      and (self.body[i - 3][:7] == '.TP\n.B '
                           or self.body[i - 3][:4] == '\n.B ')
                      ):
                    self.body[i] = '.\n'
        return ''.join(self.head + self.body + self.foot)

    def deunicode(self, text) -> None:
            """TODO: Add docstring for deunicode"""
    text = text.replace('\xa0', '\\ ')
        text = text.replace('\u2020', '\\(dg')
        return text

    def visit_Text(self, node) -> None:
            """TODO: Add docstring for visit_Text"""
    text = node.astext()
        text = text.replace('\\', '\\e')
        replace_pairs = [
            ('-', '\\-'),
            ('\'', '\\(aq'),
            ('´', "\\'"),
            ('`', '\\(ga'),
            ('"', '\\(dq'),  # double quotes are a problem on macro lines
            ]
        for (in_char, out_markup) in replace_pairs:
            text = text.replace(in_char, out_markup)
        # unicode
        text = self.deunicode(text)
        # prevent interpretation of "." at line start
        if text.startswith('.'):
            text = '\\&' + text
        if self._in_literal:
            text = text.replace('\n.', '\n\\&.')
        self.body.append(text)

    def depart_Text(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_Text"""
     """TODO: Implement depart_Text"""
    logger.debug(f"depart_Text called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_Text not yet implemented")

    def list_start(self, node) -> None:
            """TODO: Add docstring for list_start"""
    class EnumChar:
            enum_style = {
                    'bullet': '\\(bu',
                    'emdash': '\\(em',
                     }

            def __init__(self, style) -> None:
                    """TODO: Add docstring for __init__"""
    self._style = style
                if 'start' in node:
                    self._cnt = node['start'] - 1
                else:
                    self._cnt = 0
                self._indent = 2
                if style == 'arabic':
                    # indentation depends on number of children
                    # and start value.
                    self._indent = len(str(len(node.children)))
                    self._indent += len(str(self._cnt)) + 1
                elif style == 'loweralpha':
                    self._cnt += ord('a') - 1
                    self._indent = 3
                elif style == 'upperalpha':
                    self._cnt += ord('A') - 1
                    self._indent = 3
                elif style.endswith('roman'):
                    self._indent = 5

            def __next__(self) -> None:
                    """TODO: Add docstring for __next__"""
    if self._style == 'bullet':
                    return self.enum_style[self._style]
                elif self._style == 'emdash':
                    return self.enum_style[self._style]
                self._cnt += 1
                # TODO add prefix postfix
                if self._style == 'arabic':
                    return "%d." % self._cnt
                elif self._style in ('loweralpha', 'upperalpha'):
                    return "%c." % self._cnt
                elif self._style.endswith('roman'):
                    res = roman.toRoman(self._cnt) + '.'
                    if self._style.startswith('upper'):
                        return res.upper()
                    return res.lower()
                else:
                    return "%d." % self._cnt

            def get_width(self) -> Any:
                    """TODO: Add docstring for get_width"""
    return self._indent

            def __repr__(self) -> None:
                    """TODO: Add docstring for __repr__"""
    return 'enum_style-%s' % list(self._style)

        if 'enumtype' in node:
            self._list_char.append(EnumChar(node['enumtype']))
        else:
            self._list_char.append(EnumChar('bullet'))
        if len(self._list_char) > 1:
            # indent nested lists
            self.indent(self._list_char[-2].get_width())
        else:
            self.indent(self._list_char[-1].get_width())

    def list_end(self) -> None:
            """TODO: Add docstring for list_end"""
    self.dedent()
        self._list_char.pop()

    def header(self) -> None:
            """TODO: Add docstring for header"""
    th = (".TH \"%(title_upper)s\" \"%(manual_section)s\""
              " \"%(date)s\" \"%(version)s\"") % self._docinfo
        if self._docinfo["manual_group"]:
            th += " \"%(manual_group)s\"" % self._docinfo
        th += "\n"
        sh_tmpl = (".SH NAME\n"
                   "%(title)s \\- %(subtitle)s\n")
        return th + sh_tmpl % self._docinfo

    def append_header(self) -> None:
           """TODO: Add docstring for append_header"""
     """append header with .TH and .SH NAME"""
        # NOTE before everything
        # .TH title_upper section date source manual
        # BUT macros before .TH for whatis database generators.
        if self.header_written:
            return
        self.head.append(MACRO_DEF)
        self.head.append(self.header())
        self.header_written = 1

    def visit_address(self, node) -> None:
            """TODO: Add docstring for visit_address"""
    self.visit_docinfo_item(node, 'address')

    def depart_address(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_address"""
     """TODO: Implement depart_address"""
    logger.debug(f"depart_address called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_address not yet implemented")

    def visit_admonition(self, node, name=None) -> None:
            """TODO: Add docstring for visit_admonition"""
    #
        # Make admonitions a simple block quote
        # with a strong heading
        #
        # Using .IP/.RE doesn't preserve indentation
        # when admonitions contain bullets, literal,
        # and/or block quotes.
        #
        if name:
            # .. admonition:: has no name
            self.body.append('.sp\n')
            name = '%s%s:%s\n' % (
                self.defs['strong'][0],
                self.language.labels.get(name, name).upper(),
                self.defs['strong'][1],
                )
            self.body.append(name)
        self.visit_block_quote(node)

    def depart_admonition(self, node) -> None:
            """TODO: Add docstring for depart_admonition"""
    self.depart_block_quote(node)

    def visit_attention(self, node) -> None:
            """TODO: Add docstring for visit_attention"""
    self.visit_admonition(node, 'attention')

    depart_attention = depart_admonition

    def visit_docinfo_item(self, node, name) -> None:
            """TODO: Add docstring for visit_docinfo_item"""
    if name == 'author':
            self._docinfo[name].append(node.astext())
        else:
            self._docinfo[name] = node.astext()
        self._docinfo_keys.append(name)
        raise nodes.SkipNode

    def depart_docinfo_item(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_docinfo_item"""
     """TODO: Implement depart_docinfo_item"""
    logger.debug(f"depart_docinfo_item called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_docinfo_item not yet implemented")

    def visit_author(self, node) -> None:
            """TODO: Add docstring for visit_author"""
    self.visit_docinfo_item(node, 'author')

    depart_author = depart_docinfo_item

    def visit_authors(self, node) -> None:
            """TODO: Add docstring for visit_authors"""
    # _author is called anyway.
        pass

    def depart_authors(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_authors"""
     """TODO: Implement depart_authors"""
    logger.debug(f"depart_authors called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_authors not yet implemented")

    def visit_block_quote(self, node) -> None:
            """TODO: Add docstring for visit_block_quote"""
    # BUG/HACK: indent always uses the _last_ indentation,
        # thus we need two of them.
        self.indent(BLOCKQOUTE_INDENT)
        self.indent(0)

    def depart_block_quote(self, node) -> None:
            """TODO: Add docstring for depart_block_quote"""
    self.dedent()
        self.dedent()

    def visit_bullet_list(self, node) -> None:
            """TODO: Add docstring for visit_bullet_list"""
    self.list_start(node)

    def depart_bullet_list(self, node) -> None:
            """TODO: Add docstring for depart_bullet_list"""
    self.list_end()

    def visit_caption(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_caption"""
     """TODO: Implement visit_caption"""
    logger.debug(f"visit_caption called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_caption not yet implemented")

    def depart_caption(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_caption"""
     """TODO: Implement depart_caption"""
    logger.debug(f"depart_caption called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_caption not yet implemented")

    def visit_caution(self, node) -> None:
            """TODO: Add docstring for visit_caution"""
    self.visit_admonition(node, 'caution')

    depart_caution = depart_admonition

    def visit_citation(self, node) -> None:
            """TODO: Add docstring for visit_citation"""
    num = node.astext().split(None, 1)[0]
        num = num.strip()
        self.body.append('.IP [%s] 5\n' % num)

    def depart_citation(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_citation"""
     """TODO: Implement depart_citation"""
    logger.debug(f"depart_citation called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_citation not yet implemented")

    def visit_citation_reference(self, node) -> None:
            """TODO: Add docstring for visit_citation_reference"""
    self.body.append('['+node.astext()+']')
        raise nodes.SkipNode

    def visit_classifier(self, node) -> None:
            """TODO: Add docstring for visit_classifier"""
    self.body.append('(')

    def depart_classifier(self, node) -> None:
            """TODO: Add docstring for depart_classifier"""
    self.body.append(')')
        self.depart_term(node)  # close the term element after last classifier

    def visit_colspec(self, node) -> None:
            """TODO: Add docstring for visit_colspec"""
    self.colspecs.append(node)

    def depart_colspec(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_colspec"""
     """TODO: Implement depart_colspec"""
    logger.debug(f"depart_colspec called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_colspec not yet implemented")

    def write_colspecs(self) -> None:
            """TODO: Add docstring for write_colspecs"""
    self.body.append("%s.\n" % ('L '*len(self.colspecs)))

    def visit_comment(self, node,
                      sub=re.compile('-(?=-)').sub):
            """TODO: Add docstring for visit_comment"""
    self.body.append(self.comment(node.astext()))
        raise nodes.SkipNode

    def visit_contact(self, node) -> None:
            """TODO: Add docstring for visit_contact"""
    self.visit_docinfo_item(node, 'contact')

    depart_contact = depart_docinfo_item

    def visit_container(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_container"""
     """TODO: Implement visit_container"""
    logger.debug(f"visit_container called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_container not yet implemented")

    def depart_container(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_container"""
     """TODO: Implement depart_container"""
    logger.debug(f"depart_container called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_container not yet implemented")

    def visit_compound(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_compound"""
     """TODO: Implement visit_compound"""
    logger.debug(f"visit_compound called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_compound not yet implemented")

    def depart_compound(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_compound"""
     """TODO: Implement depart_compound"""
    logger.debug(f"depart_compound called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_compound not yet implemented")

    def visit_copyright(self, node) -> None:
            """TODO: Add docstring for visit_copyright"""
    self.visit_docinfo_item(node, 'copyright')

    def visit_danger(self, node) -> None:
            """TODO: Add docstring for visit_danger"""
    self.visit_admonition(node, 'danger')

    depart_danger = depart_admonition

    def visit_date(self, node) -> None:
            """TODO: Add docstring for visit_date"""
    self.visit_docinfo_item(node, 'date')

    def visit_decoration(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_decoration"""
     """TODO: Implement visit_decoration"""
    logger.debug(f"visit_decoration called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_decoration not yet implemented")

    def depart_decoration(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_decoration"""
     """TODO: Implement depart_decoration"""
    logger.debug(f"depart_decoration called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_decoration not yet implemented")

    def visit_definition(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_definition"""
     """TODO: Implement visit_definition"""
    logger.debug(f"visit_definition called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_definition not yet implemented")

    def depart_definition(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_definition"""
     """TODO: Implement depart_definition"""
    logger.debug(f"depart_definition called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_definition not yet implemented")

    def visit_definition_list(self, node) -> None:
            """TODO: Add docstring for visit_definition_list"""
    self.indent(DEFINITION_LIST_INDENT)

    def depart_definition_list(self, node) -> None:
            """TODO: Add docstring for depart_definition_list"""
    self.dedent()

    def visit_definition_list_item(self, node) -> None:
            """TODO: Add docstring for visit_definition_list_item"""
    self.body.append(self.defs['definition_list_item'][0])

    def depart_definition_list_item(self, node) -> None:
            """TODO: Add docstring for depart_definition_list_item"""
    self.body.append(self.defs['definition_list_item'][1])

    def visit_description(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_description"""
     """TODO: Implement visit_description"""
    logger.debug(f"visit_description called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_description not yet implemented")

    def depart_description(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_description"""
     """TODO: Implement depart_description"""
    logger.debug(f"depart_description called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_description not yet implemented")

    def visit_docinfo(self, node) -> None:
            """TODO: Add docstring for visit_docinfo"""
    self._in_docinfo = 1

    def depart_docinfo(self, node) -> None:
            """TODO: Add docstring for depart_docinfo"""
    self._in_docinfo = None
        # NOTE nothing should be written before this
        self.append_header()

    def visit_doctest_block(self, node) -> None:
            """TODO: Add docstring for visit_doctest_block"""
    self.body.append(self.defs['literal_block'][0])
        self._in_literal = True

    def depart_doctest_block(self, node) -> None:
            """TODO: Add docstring for depart_doctest_block"""
    self._in_literal = False
        self.body.append(self.defs['literal_block'][1])

    def visit_document(self, node) -> None:
            """TODO: Add docstring for visit_document"""
    # no blank line between comment and header.
        self.head.append(self.comment(self.document_start).rstrip()+'\n')
        # writing header is postponed
        self.header_written = 0

    def depart_document(self, node) -> None:
            """TODO: Add docstring for depart_document"""
    if self._docinfo['author']:
            self.body.append('.SH AUTHOR\n%s\n'
                             % ', '.join(self._docinfo['author']))
        skip = ('author', 'copyright', 'date',
                'manual_group', 'manual_section',
                'subtitle',
                'title', 'title_upper', 'version')
        for name in self._docinfo_keys:
            if name == 'address':
                self.body.append("\n%s:\n%s%s.nf\n%s\n.fi\n%s%s" % (
                                    self.language.labels.get(name, name),
                                    self.defs['indent'][0] % 0,
                                    self.defs['indent'][0] % BLOCKQOUTE_INDENT,
                                    self._docinfo[name],
                                    self.defs['indent'][1],
                                    self.defs['indent'][1]))
            elif name not in skip:
                if name in self._docinfo_names:
                    label = self._docinfo_names[name]
                else:
                    label = self.language.labels.get(name, name)
                self.body.append("\n%s: %s\n" % (label, self._docinfo[name]))
        if self._docinfo['copyright']:
            self.body.append('.SH COPYRIGHT\n%s\n'
                             % self._docinfo['copyright'])
        self.body.append(self.comment('Generated by docutils manpage writer.'))

    def visit_emphasis(self, node) -> None:
            """TODO: Add docstring for visit_emphasis"""
    self.body.append(self.defs['emphasis'][0])

    def depart_emphasis(self, node) -> None:
            """TODO: Add docstring for depart_emphasis"""
    self.body.append(self.defs['emphasis'][1])

    def visit_entry(self, node) -> None:
            """TODO: Add docstring for visit_entry"""
    # a cell in a table row
        if 'morerows' in node:
            self.document.reporter.warning(
                '"table row spanning" not supported', base_node=node)
        if 'morecols' in node:
            self.document.reporter.warning(
                '"table cell spanning" not supported', base_node=node)
        self.context.append(len(self.body))

    def depart_entry(self, node) -> None:
            """TODO: Add docstring for depart_entry"""
    start = self.context.pop()
        self._active_table.append_cell(self.body[start:])
        del self.body[start:]

    def visit_enumerated_list(self, node) -> None:
            """TODO: Add docstring for visit_enumerated_list"""
    self.list_start(node)

    def depart_enumerated_list(self, node) -> None:
            """TODO: Add docstring for depart_enumerated_list"""
    self.list_end()

    def visit_error(self, node) -> None:
            """TODO: Add docstring for visit_error"""
    self.visit_admonition(node, 'error')

    depart_error = depart_admonition

    def visit_field(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_field"""
     """TODO: Implement visit_field"""
    logger.debug(f"visit_field called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_field not yet implemented")

    def depart_field(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_field"""
     """TODO: Implement depart_field"""
    logger.debug(f"depart_field called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_field not yet implemented")

    def visit_field_body(self, node) -> None:
            """TODO: Add docstring for visit_field_body"""
    if self._in_docinfo:
            name_normalized = self._field_name.lower().replace(" ", "_")
            self._docinfo_names[name_normalized] = self._field_name
            self.visit_docinfo_item(node, name_normalized)
            raise nodes.SkipNode

    def depart_field_body(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_field_body"""
     """TODO: Implement depart_field_body"""
    logger.debug(f"depart_field_body called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_field_body not yet implemented")

    def visit_field_list(self, node) -> None:
            """TODO: Add docstring for visit_field_list"""
    self.indent(FIELD_LIST_INDENT)

    def depart_field_list(self, node) -> None:
            """TODO: Add docstring for depart_field_list"""
    self.dedent()

    def visit_field_name(self, node) -> None:
            """TODO: Add docstring for visit_field_name"""
    if self._in_docinfo:
            self._field_name = node.astext()
            raise nodes.SkipNode
        else:
            self.body.append(self.defs['field_name'][0])

    def depart_field_name(self, node) -> None:
            """TODO: Add docstring for depart_field_name"""
    self.body.append(self.defs['field_name'][1])

    def visit_figure(self, node) -> None:
            """TODO: Add docstring for visit_figure"""
    self.indent(2.5)
        self.indent(0)

    def depart_figure(self, node) -> None:
            """TODO: Add docstring for depart_figure"""
    self.dedent()
        self.dedent()

    def visit_footer(self, node) -> None:
            """TODO: Add docstring for visit_footer"""
    self.document.reporter.warning('"footer" not supported',
                                       base_node=node)
        # avoid output the link to document source
        raise nodes.SkipNode

    def depart_footer(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_footer"""
     """TODO: Implement depart_footer"""
    logger.debug(f"depart_footer called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_footer not yet implemented")

    def visit_footnote(self, node) -> None:
            """TODO: Add docstring for visit_footnote"""
    num, text = node.astext().split(None, 1)
        num = num.strip()
        self.body.append('.IP [%s] 5\n' % self.deunicode(num))

    def depart_footnote(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_footnote"""
     """TODO: Implement depart_footnote"""
    logger.debug(f"depart_footnote called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_footnote not yet implemented")

    def footnote_backrefs(self, node) -> None:
            """TODO: Add docstring for footnote_backrefs"""
    self.document.reporter.warning('"footnote_backrefs" not supported',
                                       base_node=node)

    def visit_footnote_reference(self, node) -> None:
            """TODO: Add docstring for visit_footnote_reference"""
    self.body.append('['+self.deunicode(node.astext())+']')
        raise nodes.SkipNode

    def depart_footnote_reference(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_footnote_reference"""
     """TODO: Implement depart_footnote_reference"""
    logger.debug(f"depart_footnote_reference called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_footnote_reference not yet implemented")

    def visit_generated(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_generated"""
     """TODO: Implement visit_generated"""
    logger.debug(f"visit_generated called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_generated not yet implemented")

    def depart_generated(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_generated"""
     """TODO: Implement depart_generated"""
    logger.debug(f"depart_generated called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_generated not yet implemented")

    def visit_header(self, node) -> None:
            """TODO: Add docstring for visit_header"""
    raise NotImplementedError(node.astext())

    def depart_header(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_header"""
     """TODO: Implement depart_header"""
    logger.debug(f"depart_header called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_header not yet implemented")

    def visit_hint(self, node) -> None:
            """TODO: Add docstring for visit_hint"""
    self.visit_admonition(node, 'hint')

    depart_hint = depart_admonition

    def visit_subscript(self, node) -> None:
            """TODO: Add docstring for visit_subscript"""
    self.body.append('\\s-2\\d')

    def depart_subscript(self, node) -> None:
            """TODO: Add docstring for depart_subscript"""
    self.body.append('\\u\\s0')

    def visit_superscript(self, node) -> None:
            """TODO: Add docstring for visit_superscript"""
    self.body.append('\\s-2\\u')

    def depart_superscript(self, node) -> None:
            """TODO: Add docstring for depart_superscript"""
    self.body.append('\\d\\s0')

    def visit_attribution(self, node) -> None:
            """TODO: Add docstring for visit_attribution"""
    self.body.append('\\(em ')

    def depart_attribution(self, node) -> None:
            """TODO: Add docstring for depart_attribution"""
    self.body.append('\n')

    def visit_image(self, node) -> None:
            """TODO: Add docstring for visit_image"""
    self.document.reporter.warning('"image" not supported',
                                       base_node=node)
        text = []
        if 'alt' in node.attributes:
            text.append(node.attributes['alt'])
        if 'uri' in node.attributes:
            text.append(node.attributes['uri'])
        self.body.append('[image: %s]\n' % ('/'.join(text)))
        raise nodes.SkipNode

    def visit_important(self, node) -> None:
            """TODO: Add docstring for visit_important"""
    self.visit_admonition(node, 'important')

    depart_important = depart_admonition

    def visit_inline(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_inline"""
     """TODO: Implement visit_inline"""
    logger.debug(f"visit_inline called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_inline not yet implemented")

    def depart_inline(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_inline"""
     """TODO: Implement depart_inline"""
    logger.debug(f"depart_inline called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_inline not yet implemented")

    def visit_label(self, node) -> None:
            """TODO: Add docstring for visit_label"""
    # footnote and citation
        if (isinstance(node.parent, nodes.footnote)
            or isinstance(node.parent, nodes.citation)):
            raise nodes.SkipNode
        self.document.reporter.warning('"unsupported "label"',
                                       base_node=node)
        self.body.append('[')

    def depart_label(self, node) -> None:
            """TODO: Add docstring for depart_label"""
    self.body.append(']\n')

    def visit_legend(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_legend"""
     """TODO: Implement visit_legend"""
    logger.debug(f"visit_legend called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_legend not yet implemented")

    def depart_legend(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_legend"""
     """TODO: Implement depart_legend"""
    logger.debug(f"depart_legend called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_legend not yet implemented")

    # WHAT should we use .INDENT, .UNINDENT ?
    def visit_line_block(self, node) -> None:
            """TODO: Add docstring for visit_line_block"""
    self._line_block += 1
        if self._line_block == 1:
            # TODO: separate inline blocks from previous paragraphs
            # see http://hg.intevation.org/mercurial/crew/rev/9c142ed9c405
            # self.body.append('.sp\n')
            # but it does not work for me.
            self.body.append('.nf\n')
        else:
            self.body.append('.in +2\n')

    def depart_line_block(self, node) -> None:
            """TODO: Add docstring for depart_line_block"""
    self._line_block -= 1
        if self._line_block == 0:
            self.body.append('.fi\n')
            self.body.append('.sp\n')
        else:
            self.body.append('.in -2\n')

    def visit_line(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_line"""
     """TODO: Implement visit_line"""
    logger.debug(f"visit_line called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_line not yet implemented")

    def depart_line(self, node) -> None:
            """TODO: Add docstring for depart_line"""
    self.body.append('\n')

    def visit_list_item(self, node) -> None:
            """TODO: Add docstring for visit_list_item"""
    # man 7 man argues to use ".IP" instead of ".TP"
        self.body.append('.IP %s %d\n' % (
                next(self._list_char[-1]),
                self._list_char[-1].get_width(),))

    def depart_list_item(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_list_item"""
     """TODO: Implement depart_list_item"""
    logger.debug(f"depart_list_item called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_list_item not yet implemented")

    def visit_literal(self, node) -> None:
            """TODO: Add docstring for visit_literal"""
    self.body.append(self.defs['literal'][0])

    def depart_literal(self, node) -> None:
            """TODO: Add docstring for depart_literal"""
    self.body.append(self.defs['literal'][1])

    def visit_literal_block(self, node) -> None:
            """TODO: Add docstring for visit_literal_block"""
    # BUG/HACK: indent always uses the _last_ indentation,
        # thus we need two of them.
        self.indent(LITERAL_BLOCK_INDENT)
        self.indent(0)
        self.body.append(self.defs['literal_block'][0])
        self._in_literal = True

    def depart_literal_block(self, node) -> None:
            """TODO: Add docstring for depart_literal_block"""
    self._in_literal = False
        self.body.append(self.defs['literal_block'][1])
        self.dedent()
        self.dedent()

    def visit_math(self, node) -> None:
            """TODO: Add docstring for visit_math"""
    self.document.reporter.warning('"math" role not supported',
                                       base_node=node)
        self.visit_literal(node)

    def depart_math(self, node) -> None:
            """TODO: Add docstring for depart_math"""
    self.depart_literal(node)

    def visit_math_block(self, node) -> None:
            """TODO: Add docstring for visit_math_block"""
    self.document.reporter.warning('"math" directive not supported',
                                       base_node=node)
        self.visit_literal_block(node)

    def depart_math_block(self, node) -> None:
            """TODO: Add docstring for depart_math_block"""
    self.depart_literal_block(node)

    # <meta> shall become an optional standard node:
    # def visit_meta(self, node) -> None:
        """TODO: Add docstring for visit_meta"""
    #     raise NotImplementedError(node.astext())

    # def depart_meta(self, node) -> None:
        """TODO: Add docstring for depart_meta"""
    #     pass

    def visit_note(self, node) -> None:
            """TODO: Add docstring for visit_note"""
    self.visit_admonition(node, 'note')

    depart_note = depart_admonition

    def indent(self, by=0.5) -> None:
            """TODO: Add docstring for indent"""
    # if we are in a section ".SH" there already is a .RS
        step = self._indent[-1]
        self._indent.append(by)
        self.body.append(self.defs['indent'][0] % step)

    def dedent(self) -> None:
            """TODO: Add docstring for dedent"""
    self._indent.pop()
        self.body.append(self.defs['indent'][1])

    def visit_option_list(self, node) -> None:
            """TODO: Add docstring for visit_option_list"""
    self.indent(OPTION_LIST_INDENT)

    def depart_option_list(self, node) -> None:
            """TODO: Add docstring for depart_option_list"""
    self.dedent()

    def visit_option_list_item(self, node) -> None:
            """TODO: Add docstring for visit_option_list_item"""
    # one item of the list
        self.body.append(self.defs['option_list_item'][0])

    def depart_option_list_item(self, node) -> None:
            """TODO: Add docstring for depart_option_list_item"""
    self.body.append(self.defs['option_list_item'][1])

    def visit_option_group(self, node) -> None:
            """TODO: Add docstring for visit_option_group"""
    # as one option could have several forms it is a group
        # options without parameter bold only, .B, -v
        # options with parameter bold italic, .BI, -f file
        #
        # we do not know if .B or .BI, blind guess:
        self.context.append('.B ')  # Add blank for sphinx (docutils/bugs/380)
        self.context.append(len(self.body))  # to be able to insert later
        self.context.append(0)               # option counter

    def depart_option_group(self, node) -> None:
            """TODO: Add docstring for depart_option_group"""
    self.context.pop()  # the counter
        start_position = self.context.pop()
        text = self.body[start_position:]
        del self.body[start_position:]
        self.body.append('%s%s\n' % (self.context.pop(), ''.join(text)))

    def visit_option(self, node) -> None:
            """TODO: Add docstring for visit_option"""
    # each form of the option will be presented separately
        if self.context[-1] > 0:
            if self.context[-3] == '.BI':
                self.body.append('\\fR,\\fB ')
            else:
                self.body.append('\\fP,\\fB ')
        if self.context[-3] == '.BI':
            self.body.append('\\')
        self.body.append(' ')

    def depart_option(self, node) -> None:
            """TODO: Add docstring for depart_option"""
    self.context[-1] += 1

    def visit_option_string(self, node) -> None:
            """TODO: Add docstring for visit_option_string"""
    # do not know if .B or .BI
        pass

    def depart_option_string(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_option_string"""
     """TODO: Implement depart_option_string"""
    logger.debug(f"depart_option_string called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_option_string not yet implemented")

    def visit_option_argument(self, node) -> None:
            """TODO: Add docstring for visit_option_argument"""
    self.context[-3] = '.BI'  # bold/italic alternate
        if node['delimiter'] != ' ':
            self.body.append('\\fB%s ' % node['delimiter'])
        elif self.body[len(self.body)-1].endswith('='):
            # a blank only means no blank in output, just changing font
            self.body.append(' ')
        else:
            # blank backslash blank, switch font then a blank
            self.body.append(' \\ ')

    def depart_option_argument(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_option_argument"""
     """TODO: Implement depart_option_argument"""
    logger.debug(f"depart_option_argument called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_option_argument not yet implemented")

    def visit_organization(self, node) -> None:
            """TODO: Add docstring for visit_organization"""
    self.visit_docinfo_item(node, 'organization')

    def depart_organization(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_organization"""
     """TODO: Implement depart_organization"""
    logger.debug(f"depart_organization called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_organization not yet implemented")

    def first_child(self, node) -> None:
            """TODO: Add docstring for first_child"""
    first = isinstance(node.parent[0], nodes.label)  # skip label
        for child in node.parent.children[first:]:
            if isinstance(child, nodes.Invisible):
                continue
            if child is node:
                return 1
            break
        return 0

    def visit_paragraph(self, node) -> None:
            """TODO: Add docstring for visit_paragraph"""
    # ``.PP`` : Start standard indented paragraph.
        # ``.LP`` : Start block paragraph, all except the first.
        # ``.P [type]``  : Start paragraph type.
        # NOTE do not use paragraph starts because they reset indentation.
        # ``.sp`` is only vertical space
        self.ensure_eol()
        if not self.first_child(node):
            self.body.append('.sp\n')
        # set in literal to escape dots after a new-line-character
        self._in_literal = True

    def depart_paragraph(self, node) -> None:
            """TODO: Add docstring for depart_paragraph"""
    self._in_literal = False
        self.body.append('\n')

    def visit_problematic(self, node) -> None:
            """TODO: Add docstring for visit_problematic"""
    self.body.append(self.defs['problematic'][0])

    def depart_problematic(self, node) -> None:
            """TODO: Add docstring for depart_problematic"""
    self.body.append(self.defs['problematic'][1])

    def visit_raw(self, node) -> None:
            """TODO: Add docstring for visit_raw"""
    if 'manpage' in node.get('format', '').split():
            self.body.append(node.astext() + "\n")
        # Keep non-manpage raw text out of output:
        raise nodes.SkipNode

    def visit_reference(self, node) -> None:
           """TODO: Add docstring for visit_reference"""
     """E.g. link or email address."""
        # .UR and .UE macros in roff use OSC8 escape sequences
        # which are not supported everywhere yet
        # therefore make the markup ourself
        if 'refuri' in node:
            # if content has the "email" do not output "mailto:email"
            if node['refuri'].endswith(node.astext()):
                self.body.append(" <")
        #TODO elif 'refid' in node:

    def depart_reference(self, node) -> None:
            """TODO: Add docstring for depart_reference"""
    if 'refuri' in node:
            # if content has the "email" do not output "mailto:email"
            if node['refuri'].endswith(node.astext()):
                self.body.append("> ")
            else:
                self.body.append(" <%s>\n" % node['refuri'])
        #TODO elif 'refid' in node:

    def visit_revision(self, node) -> None:
            """TODO: Add docstring for visit_revision"""
    self.visit_docinfo_item(node, 'revision')

    depart_revision = depart_docinfo_item

    def visit_row(self, node) -> None:
            """TODO: Add docstring for visit_row"""
    self._active_table.new_row()

    def depart_row(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_row"""
     """TODO: Implement depart_row"""
    logger.debug(f"depart_row called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_row not yet implemented")

    def visit_section(self, node) -> None:
            """TODO: Add docstring for visit_section"""
    self.section_level += 1

    def depart_section(self, node) -> None:
            """TODO: Add docstring for depart_section"""
    self.section_level -= 1

    def visit_status(self, node) -> None:
            """TODO: Add docstring for visit_status"""
    self.visit_docinfo_item(node, 'status')

    depart_status = depart_docinfo_item

    def visit_strong(self, node) -> None:
            """TODO: Add docstring for visit_strong"""
    self.body.append(self.defs['strong'][0])

    def depart_strong(self, node) -> None:
            """TODO: Add docstring for depart_strong"""
    self.body.append(self.defs['strong'][1])

    def visit_substitution_definition(self, node) -> None:
           """TODO: Add docstring for visit_substitution_definition"""
     """Internal only."""
        raise nodes.SkipNode

    def visit_substitution_reference(self, node) -> None:
            """TODO: Add docstring for visit_substitution_reference"""
    self.document.reporter.warning(
            '"substitution_reference" not supported', base_node=node)

    def visit_subtitle(self, node) -> None:
            """TODO: Add docstring for visit_subtitle"""
    if isinstance(node.parent, nodes.sidebar):
            self.body.append(self.defs['strong'][0])
        elif isinstance(node.parent, nodes.document):
            self.visit_docinfo_item(node, 'subtitle')
        elif isinstance(node.parent, nodes.section):
            self.body.append(self.defs['strong'][0])

    def depart_subtitle(self, node) -> None:
            """TODO: Add docstring for depart_subtitle"""
    # document subtitle calls SkipNode
        self.body.append(self.defs['strong'][1]+'\n.PP\n')

    def visit_system_message(self, node) -> None:
            """TODO: Add docstring for visit_system_message"""
    # TODO add report_level
        # if node['level'] < self.document.reporter['writer'].report_level:
        #    Level is too low to display:
        #    raise nodes.SkipNode
        attr = {}
        if node.hasattr('id'):
            attr['name'] = node['id']
        if node.hasattr('line'):
            line = ', line %s' % node['line']
        else:
            line = ''
        self.body.append('.IP "System Message: %s/%s (%s:%s)"\n'
                         % (node['type'], node['level'], node['source'], line))

    def depart_system_message(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_system_message"""
     """TODO: Implement depart_system_message"""
    logger.debug(f"depart_system_message called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_system_message not yet implemented")

    def visit_table(self, node) -> None:
            """TODO: Add docstring for visit_table"""
    self._active_table = Table()
        if not self._has_a_table:
            self._has_a_table = True
            # the comment to hint that preprocessor tbl should be called
            self.head.insert(0, "'\\\" t\n")  # ``'\" t`` + newline

    def depart_table(self, node) -> None:
            """TODO: Add docstring for depart_table"""
    self.ensure_eol()
        self.body.extend(self._active_table.as_list())
        self._active_table = None

    def visit_target(self, node) -> None:
            """TODO: Add docstring for visit_target"""
    # targets are in-document hyper targets, without any use for man-pages.
        raise nodes.SkipNode

    def visit_tbody(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_tbody"""
     """TODO: Implement visit_tbody"""
    logger.debug(f"visit_tbody called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_tbody not yet implemented")

    def depart_tbody(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_tbody"""
     """TODO: Implement depart_tbody"""
    logger.debug(f"depart_tbody called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_tbody not yet implemented")

    def visit_term(self, node) -> None:
            """TODO: Add docstring for visit_term"""
    self.body.append('\n.B ')

    def depart_term(self, node) -> None:
            """TODO: Add docstring for depart_term"""
    _next = node.next_node(None, descend=False, siblings=True)
        # Nest (optional) classifier(s) in the <term> element
        if isinstance(_next, nodes.classifier):
            self.body.append(' ')
            return  # skip (depart_classifier() calls this function again)
        if isinstance(_next, nodes.term):
            self.body.append('\n.TQ')
        else:
            self.body.append('\n')

    def visit_tgroup(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_tgroup"""
     """TODO: Implement visit_tgroup"""
    logger.debug(f"visit_tgroup called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_tgroup not yet implemented")

    def depart_tgroup(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_tgroup"""
     """TODO: Implement depart_tgroup"""
    logger.debug(f"depart_tgroup called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_tgroup not yet implemented")

    def visit_thead(self, node) -> None:
            """TODO: Add docstring for visit_thead"""
    # MAYBE double line '='
        pass

    def depart_thead(self, node) -> None:
            """TODO: Add docstring for depart_thead"""
    # MAYBE double line '='
        pass

    def visit_tip(self, node) -> None:
            """TODO: Add docstring for visit_tip"""
    self.visit_admonition(node, 'tip')

    depart_tip = depart_admonition

    def visit_title(self, node) -> None:
            """TODO: Add docstring for visit_title"""
    if isinstance(node.parent, nodes.topic):
            self.body.append(self.defs['topic-title'][0])
        elif isinstance(node.parent, nodes.sidebar):
            self.body.append(self.defs['sidebar-title'][0])
        elif isinstance(node.parent, nodes.admonition):
            self.body.append('.IP "')
        elif self.section_level == 0:
            self._docinfo['title'] = node.astext()
            # document title for .TH
            self._docinfo['title_upper'] = node.astext().upper()
            raise nodes.SkipNode
        elif self.section_level == 1:
            self.body.append('.SH %s\n'%self.deunicode(node.astext().upper()))
            raise nodes.SkipNode
        else:
            self.body.append('.SS ')

    def depart_title(self, node) -> None:
            """TODO: Add docstring for depart_title"""
    if isinstance(node.parent, nodes.admonition):
            self.body.append('"')
        self.body.append('\n')

    def visit_title_reference(self, node) -> None:
           """TODO: Add docstring for visit_title_reference"""
     """inline citation reference"""
        self.body.append(self.defs['title_reference'][0])

    def depart_title_reference(self, node) -> None:
            """TODO: Add docstring for depart_title_reference"""
    self.body.append(self.defs['title_reference'][1])

    def visit_topic(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_topic"""
     """TODO: Implement visit_topic"""
    logger.debug(f"visit_topic called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_topic not yet implemented")

    def depart_topic(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_topic"""
     """TODO: Implement depart_topic"""
    logger.debug(f"depart_topic called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_topic not yet implemented")

    def visit_sidebar(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_sidebar"""
     """TODO: Implement visit_sidebar"""
    logger.debug(f"visit_sidebar called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_sidebar not yet implemented")

    def depart_sidebar(*args, **kwargs) -> None:
       """TODO: Add docstring for depart_sidebar"""
     """TODO: Implement depart_sidebar"""
    logger.debug(f"depart_sidebar called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"depart_sidebar not yet implemented")

    def visit_rubric(*args, **kwargs) -> None:
       """TODO: Add docstring for visit_rubric"""
     """TODO: Implement visit_rubric"""
    logger.debug(f"visit_rubric called with args={args}, kwargs={kwargs}")
    raise NotImplementedError(f"visit_rubric not yet implemented")

    def depart_rubric(self, node) -> None:
            """TODO: Add docstring for depart_rubric"""
    self.body.append('\n')

    def visit_transition(self, node) -> None:
            """TODO: Add docstring for visit_transition"""
    # .PP      Begin a new paragraph and reset prevailing indent.
        # .sp N    leaves N lines of blank space.
        # .ce      centers the next line
        self.body.append('\n.sp\n.ce\n----\n')

    def depart_transition(self, node) -> None:
            """TODO: Add docstring for depart_transition"""
    self.body.append('\n.ce 0\n.sp\n')

    def visit_version(self, node) -> None:
            """TODO: Add docstring for visit_version"""
    self.visit_docinfo_item(node, 'version')

    def visit_warning(self, node) -> None:
            """TODO: Add docstring for visit_warning"""
    self.visit_admonition(node, 'warning')

    depart_warning = depart_admonition

    def unimplemented_visit(self, node) -> None:
            """TODO: Add docstring for unimplemented_visit"""
    raise NotImplementedError('visiting unimplemented node type: %s'
                                  % node.__class__.__name__)

# vim: set fileencoding=utf-8 et ts=4 ai :
