#!/usr/bin/env python3
"""Test script to verify the API can start."""

import pytest

pytest.skip("API not implemented", allow_module_level=True)

import os
import sys

# Add the genomevault directory to the Python path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

try:
    from genomevault.api.main import app

    print("✅ SUCCESS: API module imported successfully!")
    print(f"   App title: {app.title}")
    print(f"   App version: {app.version}")

    # Test that we can create the blockchain node
    from genomevault.blockchain.node import BlockchainNode
    from genomevault.core.constants import NodeType

    node = BlockchainNode("test", NodeType.LIGHT, False)
    print("✅ SUCCESS: BlockchainNode created successfully!")

except Exception as e:
    print(f"❌ ERROR: {type(e).__name__}: {e}")
    import traceback

    traceback.print_exc()
    sys.exit(1)





